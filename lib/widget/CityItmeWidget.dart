import 'package:flutter/material.dart';

class CityItmeWidget extends StatelessWidget {
  const CityItmeWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
